import CurrentWeatherIndicator from '../../../Components/WeatherForecast/Components/CurrentWeatherIndicator'

const WeatherNow = () => {
    return <CurrentWeatherIndicator iconSize={44} fontSize={18} />
}

export default WeatherNow
