import { css } from '@linaria/core'

const styles = css`
    height: 44px;
    background: red;
`

const SheetViewHeader = () => {
    return <div className={styles}>SheetViewHeader</div>
}

export default SheetViewHeader
